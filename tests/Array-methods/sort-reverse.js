// sort() is an array method which is used for  sorting array elements in alfabatical order

var names = ['anjali', 'kumkum', 'kiran','sarita','annie'];
console.log(names);

//SORT() METHOD
names.sort(); //this will arrang all names in alfabtical order,we can store this in an variable and create a new array
console.log(names);

var N = [10,20,30,10,5,25,50] //if we have numbers array
console.log(N);
//sort() method will arrange them on behalf of there first number 0,1,2 means it will check first degit of the number
N.sort()
console.log(N);

//Reverse() is used to reverse all values of an array 
//REVERSE() METHOD
names.reverse();

console.log(names); //we can store this in an variable and create a new array
